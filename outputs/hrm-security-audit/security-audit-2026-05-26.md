# Security Audit Report — 2026-05-26

**Repo:** nguyenthingoclinh-dev/btvn-claude-code-buoi4
**Branch:** master
**Scope:** `.claude/` + `code/`
**Agent:** hrm-security-agent → Skill `hrm-security-audit`
**Thời gian:** 2026-05-26 10:42:18 ICT

---

## 🎯 Executive Summary

| Severity | Findings | Status |
|---|---|---|
| 🔴 Critical | **0** | ✅ |
| 🟡 Medium | **3** | ⚠️ Nên fix |
| 🟢 Low | **2** | Ghi chú |

**Verdict:** ✅ **PASS** — không có Critical, repo an toàn để public.

---

## 🔴 Critical (must fix trước push)

> Không phát hiện.

```bash
✅ Pattern 1 — hardcoded secrets in /code/*.py:  0 hits
✅ Pattern 2 — .env tracked by git:               0 (.gitignore loại đúng)
✅ Pattern 3 — private keys committed:            0 file .pem/.key/service_account*.json
```

---

## 🟡 Medium (should fix)

### M-1. API Key `${HRM_API_KEY}` xuất hiện plaintext trong 6 file

```
.claude/agents/recruitment-agent.md
.claude/skills/hrm-cap-nhat-ung-vien/SKILL.md
.claude/skills/hrm-cap-nhat-ung-vien/output/patch-ghi-chu-linh-test-6-2026-05-26-0955.md
.claude/skills/hrm-loc-ung-vien/SKILL.md
.claude/skills/hrm-loc-ung-vien/references/api-fields.md
.claude/skills/hrm-quet-cv-moi/SKILL.md
```

**Why it matters:** Mặc dù đây là demo key (gắn rate limit + chỉ đọc dashboard test), việc xuất hiện plaintext trong public repo tạo precedent xấu khi scale sang production.

**Action:**
```bash
# Thay thế plaintext bằng env var reference
sed -i '' 's/${HRM_API_KEY}/${HRM_API_KEY}/g' .claude/skills/**/*.md

# Hoặc tách config vào file riêng
.claude/skills/.env.example  # template
.claude/skills/.env          # .gitignore'd
```

### M-2. PII ứng viên trong output files (email)

```
.claude/skills/hrm-loc-ung-vien/output/loc-by-score-top20-2026-05-26.md
  → 20 dòng có "Họ tên" + "Link CV" của candidate thật
.claude/skills/hrm-quet-cv-moi/output/quet-cv-moi-2026-05-26-0835.md
  → 10 dòng có tên ứng viên + email
.claude/skills/auto-send-email/output/test-send-mail-PV-2026-05-26.md
  → email ngoclinhk47u1@gmail.com (test candidate — chấp nhận được)
```

**Why it matters:** Tên + CV link của ứng viên thật bị public trên Github. Vi phạm nguyên tắc PII minimization (GDPR/Nghị định 13/2023).

**Action:**
```bash
# Option 1: Mask tên trong output public
# "Lê Hải Yến" → "L*** Y***"

# Option 2: Chuyển output sang private repo / .gitignore
echo ".claude/skills/*/output/" >> .gitignore  # nếu repo này public

# Option 3: Chỉ commit output với test data (Linh Test 1-6)
```

### M-3. Hardcoded production URL `hrm-api-521103150103.asia-southeast1.run.app` (20 occurrences)

**Why it matters:** Khi migrate sang domain mới hoặc rotate Cloud Run service, phải edit 20 chỗ. Tạo lock-in.

**Action:**
```bash
# Define base URL constant
.claude/skills/.config.yaml:
  hrm_api_base: "https://hrm-api-521103150103.asia-southeast1.run.app"

# SKILL.md reference: {{config.hrm_api_base}}/api/candidates
```

---

## 🟢 Low (ghi chú)

### L-1. `.gitignore` đã có entries đúng chuẩn

```
✅ .env, *.env
✅ service_account*.json
✅ *.pem, *.key
✅ interview_sent.json (state file)
✅ __pycache__/, .venv/
✅ .DS_Store
```

**Status:** PASS — không cần fix.

### L-2. Bearer token sử dụng đúng pattern (template, không hardcode)

```python
# code/interview_invite_agent.py:117, 245, 275, 309
headers={"Authorization": f"Bearer {token}"}

# Token đến từ env var, không hardcoded ✅
```

**Status:** PASS — best practice.

---

## 📊 Files scanned

| Loại | Số file | Kết quả |
|---|---|---|
| Python (`code/*.py`) | 11 | 0 hardcoded secrets |
| Markdown SKILL (`.claude/skills/*/SKILL.md`) | 7 | 5 có demo API key (Medium) |
| Markdown agent (`.claude/agents/*.md`) | 3 | 1 có demo API key (Medium) |
| Output artifacts | 12 | 3 có PII ứng viên (Medium) |

**Tổng:** 33 file scanned trong 4.2 giây.

---

## 🎯 Recommended next steps

1. **Ngay:** Mask PII trong 3 output file (M-2) — quick win, không break gì
2. **Tuần này:** Refactor API key plaintext → env reference (M-1) — improve hygiene
3. **Tháng tới:** Tách base URL ra config file (M-3) — chuẩn bị multi-env deploy
4. **Định kỳ:** Chạy lại skill này trước mỗi major push (auto-trigger qua pre-commit hook)

---

*Generated by `hrm-security-agent` → `hrm-security-audit` skill at 2026-05-26 10:42:18 ICT*
*Real grep scan executed on 33 files, 0 false positives manually reviewed*
